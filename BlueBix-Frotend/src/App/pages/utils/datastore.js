//import InternalPreviewGroup from "antd/lib/image/PreviewGroup";

// const Sum = (x: number, y: number): number => x + y;
// Sum(2, 3); // returns 5


// greeting();

// const Greet = (greeting: string, name?: string, name1?: string): string =>
//   greeting + " " + name + "!";

// Greet("Hello", "Steve"); //OK, returns "Hello Steve!"
// Greet("Hi"); // Compiler Error: Expected 2 arguments, but got 1.
// Greet("Hi", "Bill", "Gates"); //Compiler Error: Expected 2 arguments, but got 3.

// interface LabeledValue  {
//     key: string;
//     value: string;
// }
// const Greet112  = ({key, value}  :LabeledValue  ): string  => { return key + ' ' + value + '!'};

// let myObj = { key: "10", value: "Size 10 Object" };
// Greet112(myObj);//OK, returns "Hello Steve!"

// interface IEmployee {
//     empCode: number;
//     empName: string;
//     getSalary: (number:number) => number; // arrow function
//     getManagerName(number: string): string;
// }
